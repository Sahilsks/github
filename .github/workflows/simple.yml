name: shell Commands

on: [push]

jobs:
  run-shell-command:
    runs-on: macos-latest
    steps:
      - name: echo string
        run: eccho "hello world"
      - name: multiline script
        run: |
          node -v
          npm -v
          npx -v